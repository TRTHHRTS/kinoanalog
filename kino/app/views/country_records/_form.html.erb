<%= form_for(@country_record) do |f| %>
  <% if @country_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@country_record.errors.count, "error") %> prohibited this country_record from being saved:</h2>

      <ul>
      <% @country_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
