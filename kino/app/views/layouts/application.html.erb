<!DOCTYPE html>
<html>
<head>
  <title>PROkino</title>
  <%= stylesheet_link_tag "application.css" %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div class="container">
    <header>
      <div class="logo">
      </div>
      <section class="cabinet ">
        <% if user_signed_in? %>
            <%= render partial: 'layouts/signed_in' %>
        <% else %>
            <%= render partial: 'layouts/sign_in' %>
        <% end %>
      </section>
    </header>
    <div>
      <%= form_tag(search_result_path(:query => :query), class: '', role: 'search') do %>
      <table class="search w100perc">
        <tr>
          <td><%= text_field_tag(:query, nil, {class: 'form-control', required: 'true'}) %></td>
          <td><%= submit_tag('Найти', {class: 'btn btn-default btn-block'}) %></td>
        </tr>
      </table>
      <% end %>
    </div>
    <nav>
      <ul class="nav nav-pills nav-justified">
        <li role="presentation" class="<%= action_name == 'index' ? 'active' : '' %>"><%= link_to 'Главная', '/main'%></li>
        <li role="presentation" class="<%= action_name == 'releases' ? 'active' : '' %>"><%= link_to 'График премьер', '/releases'%></li>
        <li role="presentation" class="<%= action_name == 'random' ? 'active' : '' %>"><%= link_to 'Случайный фильм', '/random_film'%></li>
        <li role="presentation" class="<%= action_name == 'search' ? 'active' : '' %>"><%= link_to 'Расширенный поиск', '/search' %></li>
      </ul>
      <% if user_signed_in? && current_user.permission == 1 %>
          <ul class="nav nav-pills nav-justified mt-5">
            <li role="presentation" class="<%= action_name == 'new_movie' ? 'active' : '' %>"><%= link_to 'Добавить фильм', '/new_movie' %></li>
          </ul>
      <% end %>
    </nav>
    <div class="content">
      <%= yield %>
    </div>
    <footer class="panel panel-default">
      <div class="panel-body">
        <h4>Силаков, Сорокина, Исаев</h4>
      </div>
    </footer>
  </div>
</body>
</html>
