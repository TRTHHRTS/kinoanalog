<%= form_for(@genre_record) do |f| %>
  <% if @genre_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@genre_record.errors.count, "error") %> prohibited this genre_record from being saved:</h2>

      <ul>
      <% @genre_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
