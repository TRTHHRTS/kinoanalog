<% if user_signed_in? && @movie.reviews.where(user_id: current_user.id).count == 0 %>
    <%= link_to create_review_path(review: { movie_id: @movie.id , user_id: current_user.id}), remote: true, class:'btn btn-enter-color' do %>
        + Добавить отзыв
    <% end %>
<% end %>